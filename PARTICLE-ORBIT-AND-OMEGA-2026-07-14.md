# Particle orientation orbit + Omega — v3 metatag layer (2026-07-14)

This note extends the metatag lineage (2024 static particle → 2026 v2 content-addressed
self-improving PID) with a **v3 orientation layer** grounded in downstream measurements made on
2026-07-14. It follows the repository's own
[TRILATERAL evidence doctrine](TRILATERAL-PHYSICS-AND-COMPUTATION-EVIDENCE-2026-07-11.md):
every claim below is tagged, and the **no-inflate gate** is honored throughout — the measured
results are properties of *glyph-language learning on text corpora* (`MEASURED_REPO`), used as
**design-language evidence** for the particle framework. **None of it is a physical particle,
Planck-cell, or quantum-hardware measurement.**

## 1. The particle's 8-view orientation orbit  (DESIGN + code MEASURED_REPO)

The 2024 particle carried one `Spin` (Up/Down). In the running fabric a metatag's state-bytes
carry a full **C₂³ orientation orbit** — the 6-pyramid cube seen at its 8 vertices — generated by
three scales of reversal, each a genuine involution, all mutually commuting:

```text
R = reverse the byte stream            (stream scale;  Fischer black/white lineage)
N = swap the two nibbles per byte      (byte scale;    Ring-A top perspective)
Q = reverse the bits within a nibble   (nibble scale;  Ring-A bit lineage)
```

`⟨R,N,Q⟩ ≅ C₂³` (elementary-abelian; every element is its own inverse), and the full composition
`R·N·Q` is **total bit-order reversal** — the **antiparticle** (black↔white) flip as one lawful
group element. `orientation_orbit()` in `metatag_v2_behcs.py` computes the 8 content-addressed
view-PIDs and **asserts the group axioms on the particle's actual bytes** (squares, commutators,
8-way distinctness, and the R·N·Q total-reversal theorem) — the same `GROUP-GATES` receipt used
downstream. `MEASURED_REPO` (runs deterministically; the group law is proven per state).

Boundary (no-inflate): these are **encoding orientations of the metatag's bytes**, not physical
spin states, and not the 24/48-element spatial symmetry suite of a geometric cube (that is a
separate, later gate).

## 2. Isotropy at depth  (MEASURED_REPO — computational, not physical)

Downstream on real enwik9 text, two distinct samplings were run and their completion status
differs — kept separate to avoid lending completeness credit between them:

- **Contiguous-neighborhood sampling — COMPLETE (8/8 views).** All 8 orientation views trained to
  full depth (8 views × 27 cubes × 800 uniform passes = 172,800 passes, `accepted=all, held=0`,
  `restore=exact` = a deterministic in-memory byte round-trip, **not a hardware error rate**). This
  is the source of the ±0.26% convergence below.
- **Full-corpus (wide) sampling — PARTIAL (4/8 views at time of writing).** Source of the ±0.19% /
  ~18%-offset figures in the last paragraph. Not a closed 8-view result yet.

For the complete contiguous run, the learned per-view densities **converge**:

```text
100 uniform passes:  orientation matters strongly  (relative spread ≈ 42.5%, one view sweeps)
800 uniform passes:  the orbit CONVERGES           (half-range ≈ ±0.26%, cohort ≈ 585k bytes)
```

This is a measured computational analog of the design-language phrase *"expands equally into every
pyramid, universally balancing out"*: at depth, orientation advantage is a **transient of early
training** and the cube learns near-equal density from every face. **Evidence tier `MEASURED_REPO`
— measured in bytes-of-learned-glyph-language on text, NOT in any physical space.** Equal density
is not identical law: the per-view catalogs still differ (a separate decoded-law conjugacy gate
remains open).

Independent corroboration: full-corpus sampling (27 cubes spread across the whole gigabyte) held
the same isotropy to ≈0.19% across the four completed wide views, while running ~18% lower in
absolute density than the contiguous neighborhood sample — position sets *how much* is learned, not
*the isotropy*. The two effects are cleanly separable. (`MEASURED_REPO`; partial — 4/8 wide views.)

## 3. The Omega bit  (DESIGN + code MEASURED_REPO)

`omega_commitment(metatags, epoch)` mints the **seven-node center** (6 apex sectors + 1 Ω): a
Merkle-style commitment root over the sorted sector PIDs. Ω is **explicitly not an oracle** of
unobserved futures — it commits only the causally-available sector states, is recomputable by
anyone, and may seed epoch+1 **without a causal loop** (sectors update → Ω recomputed → sealed; Ω
cannot retroactively alter the epoch it committed). This names and seals what `big_loop`'s "all
pixels JOIN" already gestured at. The first downstream Ω over
a complete 8-view orbit was `537dc90b…0123d444`, committing the eight `FIRST-FLOOR-RESULT.hbp`
digests of GitHub run `29339920613` (Liris-seat recompute; third-seat Relic recompute pending).
The `V3OMEGA` value in the sidecar commits a different, smaller leaf-set — the two demo-electron
PIDs of `metatag_v2_behcs.py` — and is reproducible directly from this repo. `MEASURED_REPO`.

## 4. Entanglement = shared key  (DESIGN, external anchor MEASURED_EXTERNAL)

The v2 `entangle()` (bilateral cosign, `PhaseCorrelation 1.0 ⇔ byte-exact sha match`) and the v3
`antiparticle()` (source and flip share one `shared_key_sha256`) together express *"reprojections
share the same quantum key"*. This rhymes with an externally-cited **quantum encrypted-cloning**
result (arXiv 2602.10695: encrypted clone locally mixed; clone+full key → exact recovery; key
consumed → no second readable recovery), which lives **inside** the no-cloning theorem. `CANNOT_SEE`
from the Liris seat: this citation is **not verified here** — whether it is an experimental
measurement or a theory/protocol proposal is unconfirmed from this vantage, so its `MEASURED_EXTERNAL`
status is *claimed by the external source, not re-measured by us*. Per the no-inflate gate it is
**not a completed Asolaria quantum network**, and the cosign here is a classical sha match, not a
physical entangled pair.

## Evidence ledger

```text
C2^3 orientation orbit + R.N.Q antiparticle   DESIGN + MEASURED_REPO        (group law proven per state)
isotropy at depth (~±0.26%, 8/8 views)         MEASURED_REPO                 (glyph-language on enwik9 text)
corpus-geography density (~18%, isotropy held) MEASURED_REPO (partial 4/8 wide views)
Omega commitment (Merkle root, not oracle)     DESIGN + MEASURED_REPO
entanglement = shared-key cosign               DESIGN
encrypted-cloning external citation            CANNOT_SEE (external source claims MEASURED_EXTERNAL; not re-measured here)
physical particle / Planck-cell / quantum net  UNVERIFIED_LIVE  (held; not claimed)
```

Trilateral: this note is the Liris-seat authoring pass. Acer origin evidence and an independent
third-seat (Relic) attack-verification are preserved as separate receipts; HBP/HBI/SHA sidecars
accompany this file for independent recomputation.
